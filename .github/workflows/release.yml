name: Build Debian Package

on:
  push:
    tags:
      - "v*"   
  workflow_dispatch:

permissions:
  contents: write  

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install build dependencies
        run: |
          sudo apt update
          sudo apt install -y build-essential devscripts debhelper dpkg-dev

      - name: Build .deb with dpkg-buildpackage
        run: |
          dpkg-buildpackage -us -uc

      - name: Move .deb files to artifacts
        run: |
          mkdir -p artifacts
          cp ../*.deb artifacts/
          cp ../*.changes artifacts/ || true
          cp ../*.buildinfo artifacts/ || true

      - name: Create GitHub Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref_name }}
          release_name: Release ${{ github.ref_name }}
          draft: false
          prerelease: false

      - name: Upload .deb to Release
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: artifacts/plymouth-theme-sirros_1.1_all.deb
          asset_name: plymouth-theme-sirros_1.1_all.deb
          asset_content_type: application/vnd.debian.binary-package
